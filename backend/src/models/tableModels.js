const DB_Connection = require("../database/db.js");
const { runWithLogging } = require("../utils/runWithLogging.js");

class TableModel {
    constructor() {
        this.db = DB_Connection.getInstance();
    }

    async createEnums() {
        const sql = `
            -- ENUMs
            DO $$ BEGIN
                IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'role_enum') THEN
                    CREATE TYPE role_enum AS ENUM ('student', 'teacher', 'system');
                END IF;
                IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'student_status_enum') THEN
                    CREATE TYPE student_status_enum AS ENUM ('Active', 'Graduated', 'Paused');
                END IF;
                IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'course_type_enum') THEN
                    CREATE TYPE course_type_enum AS ENUM ('Theory', 'Lab');
                END IF;
                IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'enrollment_status_enum') THEN
                    CREATE TYPE enrollment_status_enum AS ENUM ('Pending', 'Enrolled', 'Withdrawn');
                END IF;
            END $$;
            
            DROP TYPE IF EXISTS marking_type_enum CASCADE;
            CREATE TYPE marking_type_enum AS ENUM ('Midterm', 'Final', 'CT', 'Attendance');
            
            DO $$ BEGIN
                IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'marking_status_enum') THEN
                    CREATE TYPE marking_status_enum AS ENUM ('Published', 'Draft');
                END IF;
                IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'payment_method_enum') THEN
                    CREATE TYPE payment_method_enum AS ENUM ('Mobile Banking', 'Bank Transfer');
                END IF;
                IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'payment_status_enum') THEN
                    CREATE TYPE payment_status_enum AS ENUM ('Paid', 'Partial', 'Overdue');
                END IF;
                IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'appointment_type_enum') THEN
                    CREATE TYPE appointment_type_enum AS ENUM ('Department Head', 'Professor', 'Assistant Professor', 'Lecturer', 'Adjunct Faculty');
                END IF;
                IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'due_frequency_enum') THEN
                    CREATE TYPE due_frequency_enum AS ENUM ('per_term', 'per_year', 'one_time');
                END IF;
            END $$;
        `;
        return await runWithLogging(() => this.db.query(sql), "Creating Enums");
    }

    async createCoreUsers() {
        const sql = `
            CREATE TABLE IF NOT EXISTS users (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                name VARCHAR(255) NOT NULL,
                email VARCHAR(255) UNIQUE NOT NULL,
                password_hash VARCHAR(255) NOT NULL,
                role role_enum DEFAULT 'student',
                refresh_token TEXT,
                mobile_number VARCHAR(20),
                mobile_banking_number VARCHAR(20),
                bank_account_number VARCHAR(50),
                permanent_address TEXT,
                present_address TEXT NOT NULL,
                birth_reg_number VARCHAR(50),
                nid_number VARCHAR(50),
                passport_number VARCHAR(50),
                birth_date DATE NOT NULL,
                emergency_contact_name VARCHAR(255),
                emergency_contact_number VARCHAR(20),
                emergency_contact_relation VARCHAR(50),
                profile_image_url TEXT
            );

            ALTER TABLE users
            ADD COLUMN IF NOT EXISTS profile_image_url TEXT;

            CREATE TABLE IF NOT EXISTS initial_credentials (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                user_id INTEGER REFERENCES users(id) ON DELETE CASCADE UNIQUE NOT NULL,
                user_name VARCHAR(255) NOT NULL,
                raw_password VARCHAR(255) NOT NULL,
                has_changed BOOLEAN DEFAULT FALSE
            );
        `;
        return await runWithLogging(() => this.db.query(sql), "Creating Core Users Tables");
    }

    async createOrganizations() {
        const sql = `
            CREATE TABLE IF NOT EXISTS departments (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                code VARCHAR(20) UNIQUE,
                name VARCHAR(255) NOT NULL,
                department_head_id INT
            );
        `;
        return await runWithLogging(() => this.db.query(sql), "Creating Organizations Tables");
    }

    async createAcademics() {
        const sql = `
            CREATE TABLE IF NOT EXISTS terms (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                term_number INT NOT NULL,
                start_date DATE NOT NULL,
                end_date DATE NOT NULL,
                max_credit DECIMAL(4, 1) NOT NULL DEFAULT 23.0,
                department_id INTEGER REFERENCES departments(id) NOT NULL,
                UNIQUE (term_number, department_id, start_date),
                CONSTRAINT terms_max_credit_positive_chk CHECK (max_credit > 0)
            );

            CREATE TABLE IF NOT EXISTS teachers (
                user_id INT PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
                appointment appointment_type_enum,
                official_mail VARCHAR(255),
                department_id INT REFERENCES departments(id)
            );

            CREATE TABLE IF NOT EXISTS students (
                user_id INT PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
                roll_number VARCHAR(20) UNIQUE NOT NULL,
                official_mail VARCHAR(255),
                status student_status_enum DEFAULT 'Active',
                current_term INTEGER REFERENCES terms(id)
            );

            CREATE TABLE IF NOT EXISTS department_heads (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                department_id INTEGER REFERENCES departments(id) ON DELETE CASCADE,
                teacher_id INTEGER REFERENCES teachers(user_id) ON DELETE CASCADE,
                start_date DATE NOT NULL,
                end_date DATE,
                CONSTRAINT department_heads_date_window_chk CHECK (end_date IS NULL OR end_date >= start_date)
            );

            CREATE INDEX IF NOT EXISTS idx_department_heads_department_dates
                ON department_heads (department_id, start_date DESC, end_date DESC);

            CREATE UNIQUE INDEX IF NOT EXISTS uq_department_heads_current_per_department
                ON department_heads (department_id)
                WHERE end_date IS NULL;

            CREATE TABLE IF NOT EXISTS student_term_history (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                student_id INTEGER REFERENCES students(user_id) ON DELETE CASCADE,
                term_id INTEGER REFERENCES terms(id) ON DELETE CASCADE,
                start_date DATE,
                end_date DATE
            );

            CREATE TABLE IF NOT EXISTS courses (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                course_code VARCHAR(20) UNIQUE NOT NULL, 
                name VARCHAR(255) NOT NULL,
                credit_hours DECIMAL(3, 1) NOT NULL,
                type course_type_enum,
                department_id INTEGER REFERENCES departments(id) NOT NULL
            );

            CREATE TABLE IF NOT EXISTS course_prerequisites (
                course_id INTEGER REFERENCES courses(id),
                prereq_id INTEGER REFERENCES courses(id),
                PRIMARY KEY (course_id, prereq_id)
            );

            CREATE TABLE IF NOT EXISTS course_offerings (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                term_id INT REFERENCES terms(id) NOT NULL,
                course_id INTEGER REFERENCES courses(id) NOT NULL,
                max_capacity INT,
                is_optional BOOLEAN NOT NULL DEFAULT FALSE,
                is_active BOOLEAN NOT NULL DEFAULT TRUE,
                UNIQUE(term_id, course_id)
            );

            CREATE INDEX IF NOT EXISTS idx_course_offerings_term_optional
                ON course_offerings (term_id, is_optional);

            CREATE TABLE IF NOT EXISTS sections (
                term_id INTEGER REFERENCES terms(id), 
                name VARCHAR(50),
                UNIQUE(term_id, name)
            );

            CREATE TABLE IF NOT EXISTS teaches (
                course_offering_id INTEGER REFERENCES course_offerings(id),
                teacher_id INTEGER REFERENCES teachers(user_id),
                section_name VARCHAR(50)
            );

            CREATE UNIQUE INDEX IF NOT EXISTS uq_teaches_offering_section
                ON teaches (course_offering_id, section_name);

            CREATE INDEX IF NOT EXISTS idx_teaches_teacher
                ON teaches (teacher_id);

            CREATE TABLE IF NOT EXISTS student_sections (
                student_id INTEGER REFERENCES students(user_id),
                section_name VARCHAR(50)
            );

            CREATE UNIQUE INDEX IF NOT EXISTS uq_student_sections_student
                ON student_sections (student_id);

            CREATE INDEX IF NOT EXISTS idx_student_sections_section_name
                ON student_sections (section_name);

            CREATE TABLE IF NOT EXISTS student_advisor_history (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                student_id INT REFERENCES students(user_id),
                teacher_id INT REFERENCES teachers(user_id),
                start_date DATE NOT NULL,
                end_date DATE,
                change_reason TEXT,
                CONSTRAINT student_advisor_history_date_window_chk CHECK (end_date IS NULL OR end_date >= start_date)
            );

            CREATE INDEX IF NOT EXISTS idx_student_advisor_history_student_dates
                ON student_advisor_history (student_id, start_date DESC, end_date DESC);

            CREATE INDEX IF NOT EXISTS idx_student_advisor_history_teacher_dates
                ON student_advisor_history (teacher_id, start_date DESC, end_date DESC);

            CREATE UNIQUE INDEX IF NOT EXISTS uq_student_advisor_history_current_per_student
                ON student_advisor_history (student_id)
                WHERE end_date IS NULL;
        `;
        return await runWithLogging(() => this.db.query(sql), "Creating Academics Tables");
    }

    async createGrading() {
        const sql = `
            CREATE TABLE IF NOT EXISTS student_enrollments (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                student_id INT REFERENCES students(user_id),
                course_offering_id INT REFERENCES course_offerings(id),
                credit_when_taking DECIMAL(3, 1),
                status enrollment_status_enum DEFAULT 'Pending',
                enrollment_timestamp TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
                approved_timestamp TIMESTAMPTZ,
                grade VARCHAR(5),
                is_retake BOOLEAN DEFAULT FALSE,
                approved_by_teacher_id INT REFERENCES teachers(user_id)
            );

            CREATE TABLE IF NOT EXISTS marking_components (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                enrollment_id INT REFERENCES student_enrollments(id) ON DELETE CASCADE,
                type marking_type_enum,
                total_marks DECIMAL(5, 2),
                marks_obtained DECIMAL(5, 2),
                status marking_status_enum DEFAULT 'Draft'
            );
        `;
        return await runWithLogging(() => this.db.query(sql), "Creating Grading Tables");
    }

    async createDues() {
        const sql = `
            CREATE TABLE IF NOT EXISTS dues (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                name VARCHAR(100), 
                amount DECIMAL(10, 2) NOT NULL,
                bank_account_number VARCHAR(50),
                is_active BOOLEAN DEFAULT TRUE,
                required_for_registration BOOLEAN DEFAULT TRUE,
                description TEXT
            );

            CREATE TABLE IF NOT EXISTS student_dues_payment (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                student_id INT REFERENCES students(user_id),
                due_id INT REFERENCES dues(id),
                amount_paid DECIMAL(10, 2) DEFAULT 0,
                amount_due_override DECIMAL(10, 2),
                paid_at TIMESTAMPTZ,
                payment_method payment_method_enum,
                mobile_banking_number VARCHAR(20),
                status payment_status_enum DEFAULT 'Overdue',
                deadline DATE,
                term_id INT REFERENCES terms(id),
                issued_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
                due_date DATE,
                required_for_registration BOOLEAN DEFAULT TRUE,
                waived_at TIMESTAMPTZ,
                waive_reason TEXT
            );

            CREATE INDEX IF NOT EXISTS idx_student_dues_blocking_lookup
                ON student_dues_payment (student_id, term_id, required_for_registration, status);

            CREATE INDEX IF NOT EXISTS idx_student_dues_due_date
                ON student_dues_payment (due_date);

            CREATE TABLE IF NOT EXISTS due_rules (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                name VARCHAR(255) NOT NULL,
                due_id INT REFERENCES dues(id) ON DELETE CASCADE,
                frequency due_frequency_enum NOT NULL DEFAULT 'per_year',
                is_active BOOLEAN NOT NULL DEFAULT TRUE,
                required_for_registration BOOLEAN NOT NULL DEFAULT TRUE,
                issue_offset_days INT NOT NULL DEFAULT 7,
                starts_on DATE,
                ends_on DATE,
                created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
                updated_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
                CONSTRAINT due_rules_window_chk CHECK (starts_on IS NULL OR ends_on IS NULL OR starts_on <= ends_on)
            );

            CREATE TABLE IF NOT EXISTS due_rule_scopes (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                rule_id INT NOT NULL REFERENCES due_rules(id) ON DELETE CASCADE,
                department_id INT REFERENCES departments(id),
                term_number INT,
                section_name VARCHAR(50),
                batch_year INT,
                created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
            );

            CREATE INDEX IF NOT EXISTS idx_due_rule_scopes_match
                ON due_rule_scopes (rule_id, department_id, term_number, section_name, batch_year);

            CREATE TABLE IF NOT EXISTS due_rule_amount_overrides (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                rule_id INT NOT NULL REFERENCES due_rules(id) ON DELETE CASCADE,
                department_id INT REFERENCES departments(id),
                term_number INT,
                section_name VARCHAR(50),
                batch_year INT,
                override_amount DECIMAL(10, 2) NOT NULL,
                created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP
            );

            CREATE INDEX IF NOT EXISTS idx_due_rule_amount_overrides_match
                ON due_rule_amount_overrides (rule_id, department_id, term_number, section_name, batch_year);

            CREATE TABLE IF NOT EXISTS due_rule_issuance_log (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                rule_id INT REFERENCES due_rules(id) ON DELETE SET NULL,
                student_id INT NOT NULL REFERENCES students(user_id) ON DELETE CASCADE,
                term_id INT REFERENCES terms(id) ON DELETE SET NULL,
                student_due_payment_id INT REFERENCES student_dues_payment(id) ON DELETE SET NULL,
                note TEXT,
                created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
                UNIQUE (rule_id, student_id, term_id)
            );
        `;
        return await runWithLogging(() => this.db.query(sql), "Creating Dues Tables");
    }

    async createFeedback() {
        const sql = `
            CREATE TABLE IF NOT EXISTS feedback (
                id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
                course_offering_id INTEGER REFERENCES course_offerings(id),
                teacher_id INT REFERENCES teachers(user_id), 
                csv_file_url TEXT,
                average_rating DECIMAL(3, 2)
            );
        `;
        return await runWithLogging(() => this.db.query(sql), "Creating Feedback Tables");
    }

    async createSystemState() {
        const sql = `
            CREATE TABLE IF NOT EXISTS current_state (
                id SMALLINT PRIMARY KEY DEFAULT 1,
                reg_start DATE,
                reg_end DATE,
                newest_term_start DATE,
                term_start DATE,
                term_end DATE,
                session_end_status VARCHAR(20) NOT NULL DEFAULT 'idle',
                session_end_started_at TIMESTAMPTZ,
                session_end_completed_at TIMESTAMPTZ,
                session_end_started_by INT REFERENCES users(id),
                session_end_error TEXT,
                session_end_summary JSONB,
                last_ended_term_start DATE,
                last_ended_term_end DATE,
                last_ended_next_term_start DATE,
                updated_at TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP,
                updated_by INT REFERENCES users(id),
                CONSTRAINT current_state_singleton_chk CHECK (id = 1),
                CONSTRAINT current_state_reg_window_chk CHECK (reg_start IS NULL OR reg_end IS NULL OR reg_start <= reg_end),
                CONSTRAINT current_state_term_window_chk CHECK (term_start IS NULL OR term_end IS NULL OR term_start <= term_end),
                CONSTRAINT current_state_session_end_status_chk CHECK (session_end_status IN ('idle', 'running', 'completed', 'failed'))
            );

            DO $$
            DECLARE
                v_keep_ctid tid;
            BEGIN
                IF NOT EXISTS (SELECT 1 FROM current_state) THEN
                    INSERT INTO current_state (id) VALUES (1);
                END IF;
            END $$;

            CREATE TABLE IF NOT EXISTS current_state_history (
                id BIGSERIAL PRIMARY KEY,
                changed_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP,
                changed_by INT REFERENCES users(id),
                old_reg_start DATE,
                new_reg_start DATE,
                old_reg_end DATE,
                new_reg_end DATE,
                old_term_start DATE,
                new_term_start DATE,
                old_term_end DATE,
                new_term_end DATE,
                old_newest_term_start DATE,
                new_newest_term_start DATE,
                reason TEXT
            );
        `;
        return await runWithLogging(() => this.db.query(sql), "Creating System State Tables");
    }

    async createFunctions() {
        const sql = `
            -- Trigger function to enforce limits per enrollment (only Published)
            CREATE OR REPLACE FUNCTION trg_marking_components_limits()
            RETURNS TRIGGER
            LANGUAGE plpgsql
            AS $$
            DECLARE
                existing_ct_count INT;
                existing_att_count INT;
                existing_final_count INT;
                exclude_id INT := NULL;
            BEGIN
                IF TG_OP = 'UPDATE' THEN
                    exclude_id := OLD.id;
                END IF;

                -- Count existing Published components for the same enrollment, excluding the current row if updating
                SELECT COUNT(*) INTO existing_ct_count
                FROM marking_components
                WHERE enrollment_id = NEW.enrollment_id
                  AND status = 'Published'
                  AND type = 'CT'
                  AND (id IS DISTINCT FROM exclude_id);

                SELECT COUNT(*) INTO existing_att_count
                FROM marking_components
                WHERE enrollment_id = NEW.enrollment_id
                  AND status = 'Published'
                  AND type = 'Attendance'
                  AND (id IS DISTINCT FROM exclude_id);

                SELECT COUNT(*) INTO existing_final_count
                FROM marking_components
                WHERE enrollment_id = NEW.enrollment_id
                  AND status = 'Published'
                  AND type = 'Final'
                  AND (id IS DISTINCT FROM exclude_id);

                -- If NEW will be Published, check the incremented counts
                IF NEW.status = 'Published' THEN
                    IF NEW.type = 'CT' THEN
                        IF existing_ct_count + 1 > 4 THEN
                            RAISE EXCEPTION 'Limit exceeded: enrollment_id=% already has % Published CT components; max allowed is 4.',
                                NEW.enrollment_id, existing_ct_count;
                        END IF;
                    ELSIF NEW.type = 'Attendance' THEN
                        IF existing_att_count + 1 > 1 THEN
                            RAISE EXCEPTION 'Limit exceeded: enrollment_id=% already has % Published Attendance components; max allowed is 1.',
                                NEW.enrollment_id, existing_att_count;
                        END IF;
                    ELSIF NEW.type = 'Final' THEN
                        IF existing_final_count + 1 > 1 THEN
                            RAISE EXCEPTION 'Limit exceeded: enrollment_id=% already has % Published Final components; max allowed is 1.',
                                NEW.enrollment_id, existing_final_count;
                        END IF;
                    ELSE
                        -- For other types, still ensure existing data is not already violating limits
                        IF existing_ct_count > 4 THEN
                            RAISE EXCEPTION 'Existing data violation: enrollment_id=% has % Published CT components (>4).', NEW.enrollment_id, existing_ct_count;
                        END IF;
                        IF existing_att_count > 1 THEN
                            RAISE EXCEPTION 'Existing data violation: enrollment_id=% has % Published Attendance components (>1).', NEW.enrollment_id, existing_att_count;
                        END IF;
                        IF existing_final_count > 1 THEN
                            RAISE EXCEPTION 'Existing data violation: enrollment_id=% has % Published Final components (>1).', NEW.enrollment_id, existing_final_count;
                        END IF;
                    END IF;
                ELSE
                    -- NEW is not Published: still block if existing published rows already violate limits
                    IF existing_ct_count > 4 THEN
                        RAISE EXCEPTION 'Existing data violation: enrollment_id=% has % Published CT components (>4).', NEW.enrollment_id, existing_ct_count;
                    END IF;
                    IF existing_att_count > 1 THEN
                        RAISE EXCEPTION 'Existing data violation: enrollment_id=% has % Published Attendance components (>1).', NEW.enrollment_id, existing_att_count;
                    END IF;
                    IF existing_final_count > 1 THEN
                        RAISE EXCEPTION 'Existing data violation: enrollment_id=% has % Published Final components (>1).', NEW.enrollment_id, existing_final_count;
                    END IF;
                END IF;

                RETURN NEW;
            END;
            $$;

            -- Attach the trigger to marking_components
            DROP TRIGGER IF EXISTS marking_components_limits_trg ON marking_components;

            CREATE TRIGGER marking_components_limits_trg
            BEFORE INSERT OR UPDATE ON marking_components
            FOR EACH ROW
            EXECUTE FUNCTION trg_marking_components_limits();

            CREATE OR REPLACE FUNCTION compile_student_term_result(p_student_id INT, p_term_id INT)
            RETURNS TABLE (
                enrollment_id INT,
                course_offering_id INT,
                course_id INT,
                course_code VARCHAR,
                course_name VARCHAR,
                ct_best3_score NUMERIC(8,2),
                attendance_score NUMERIC(8,2),
                final_score NUMERIC(8,2),
                total_score NUMERIC(8,2),
                percentage NUMERIC(5,2),
                grade VARCHAR
            )
            LANGUAGE sql
            AS $$
            WITH enrolls AS (
                SELECT se.id AS enrollment_id,
                       se.course_offering_id
                FROM student_enrollments se
                JOIN course_offerings co ON se.course_offering_id = co.id
                WHERE se.student_id = p_student_id
                  AND co.term_id = p_term_id
            ),
            published_marks AS (
                SELECT mc.id,
                       mc.enrollment_id,
                       mc.type,
                       COALESCE(mc.total_marks, 0) AS total_marks,
                       COALESCE(mc.marks_obtained, 0) AS marks_obtained,
                       CASE
                         WHEN mc.type = 'CT' THEN
                           CASE WHEN COALESCE(mc.total_marks,0) = 0 THEN 0
                                ELSE (COALESCE(mc.marks_obtained,0)::numeric / COALESCE(mc.total_marks,0)::numeric) * 20.0
                           END
                         WHEN mc.type = 'Attendance' THEN
                           CASE WHEN COALESCE(mc.total_marks,0) = 0 THEN 0
                                ELSE (COALESCE(mc.marks_obtained,0)::numeric / COALESCE(mc.total_marks,0)::numeric) * 30.0
                           END
                         WHEN mc.type = 'Final' THEN
                           CASE WHEN COALESCE(mc.total_marks,0) = 0 THEN 0
                                ELSE (COALESCE(mc.marks_obtained,0)::numeric / COALESCE(mc.total_marks,0)::numeric) * 210.0
                           END
                         ELSE 0
                       END AS normalized_score
                FROM marking_components mc
                JOIN enrolls e ON mc.enrollment_id = e.enrollment_id
                WHERE mc.status = 'Published'
            ),
            ct_ranked AS (
                SELECT pm.*,
                       ROW_NUMBER() OVER (PARTITION BY pm.enrollment_id ORDER BY pm.normalized_score DESC, pm.id) AS rn
                FROM published_marks pm
                WHERE pm.type = 'CT'
            ),
            ct_best3 AS (
                SELECT enrollment_id,
                       COALESCE(SUM(normalized_score),0)::numeric(8,2) AS ct_best3_score
                FROM ct_ranked
                WHERE rn <= 3
                GROUP BY enrollment_id
            ),
            attendance_sum AS (
                SELECT enrollment_id,
                       COALESCE(SUM(normalized_score),0)::numeric(8,2) AS attendance_score
                FROM published_marks
                WHERE type = 'Attendance'
                GROUP BY enrollment_id
            ),
            final_sum AS (
                SELECT enrollment_id,
                       COALESCE(SUM(normalized_score),0)::numeric(8,2) AS final_score
                FROM published_marks
                WHERE type = 'Final'
                GROUP BY enrollment_id
            )
            SELECT
                e.enrollment_id,
                e.course_offering_id,
                c.id AS course_id,
                c.course_code,
                c.name AS course_name,
                COALESCE(ct.ct_best3_score, 0) AS ct_best3_score,
                COALESCE(att.attendance_score, 0) AS attendance_score,
                COALESCE(fin.final_score, 0) AS final_score,
                (COALESCE(ct.ct_best3_score,0) + COALESCE(att.attendance_score,0) + COALESCE(fin.final_score,0))::numeric(8,2) AS total_score,
                ROUND( ( (COALESCE(ct.ct_best3_score,0) + COALESCE(att.attendance_score,0) + COALESCE(fin.final_score,0)) / 300.0 ) * 100.0, 2) AS percentage,
                CASE
                  WHEN ( (COALESCE(ct.ct_best3_score,0) + COALESCE(att.attendance_score,0) + COALESCE(fin.final_score,0)) / 300.0 ) * 100.0 >= 80 THEN 'A+'
                  WHEN ( (COALESCE(ct.ct_best3_score,0) + COALESCE(att.attendance_score,0) + COALESCE(fin.final_score,0)) / 300.0 ) * 100.0 >= 75 THEN 'A'
                  WHEN ( (COALESCE(ct.ct_best3_score,0) + COALESCE(att.attendance_score,0) + COALESCE(fin.final_score,0)) / 300.0 ) * 100.0 >= 70 THEN 'A-'
                  WHEN ( (COALESCE(ct.ct_best3_score,0) + COALESCE(att.attendance_score,0) + COALESCE(fin.final_score,0)) / 300.0 ) * 100.0 >= 65 THEN 'B'
                  WHEN ( (COALESCE(ct.ct_best3_score,0) + COALESCE(att.attendance_score,0) + COALESCE(fin.final_score,0)) / 300.0 ) * 100.0 >= 60 THEN 'C'
                  WHEN ( (COALESCE(ct.ct_best3_score,0) + COALESCE(att.attendance_score,0) + COALESCE(fin.final_score,0)) / 300.0 ) * 100.0 >= 55 THEN 'D'
                  ELSE 'F'
                END AS grade
            FROM enrolls e
            LEFT JOIN courses c ON c.id = (SELECT co.course_id FROM course_offerings co WHERE co.id = e.course_offering_id)
            LEFT JOIN ct_best3 ct ON ct.enrollment_id = e.enrollment_id
            LEFT JOIN attendance_sum att ON att.enrollment_id = e.enrollment_id
            LEFT JOIN final_sum fin ON fin.enrollment_id = e.enrollment_id
            ORDER BY c.course_code;
            $$;

            CREATE OR REPLACE FUNCTION refresh_enrollment_grade_from_markings(p_enrollment_id INT)
            RETURNS VOID
            LANGUAGE plpgsql
            AS $$
            DECLARE
                v_student_id INT;
                v_term_id INT;
                v_grade VARCHAR;
            BEGIN
                SELECT se.student_id, co.term_id
                INTO v_student_id, v_term_id
                FROM student_enrollments se
                JOIN course_offerings co ON co.id = se.course_offering_id
                WHERE se.id = p_enrollment_id;

                IF NOT FOUND THEN
                    RETURN;
                END IF;

                SELECT csr.grade
                INTO v_grade
                FROM compile_student_term_result(v_student_id, v_term_id) csr
                WHERE csr.enrollment_id = p_enrollment_id
                LIMIT 1;

                UPDATE student_enrollments
                SET grade = v_grade
                WHERE id = p_enrollment_id;
            END;
            $$;

            CREATE OR REPLACE FUNCTION trg_sync_enrollment_grade_from_markings()
            RETURNS TRIGGER
            LANGUAGE plpgsql
            AS $$
            BEGIN
                IF TG_OP = 'UPDATE' AND OLD.enrollment_id IS DISTINCT FROM NEW.enrollment_id THEN
                    PERFORM refresh_enrollment_grade_from_markings(OLD.enrollment_id);
                END IF;

                IF TG_OP = 'DELETE' THEN
                    PERFORM refresh_enrollment_grade_from_markings(OLD.enrollment_id);
                    RETURN OLD;
                END IF;

                PERFORM refresh_enrollment_grade_from_markings(NEW.enrollment_id);
                RETURN NEW;
            END;
            $$;

            DROP TRIGGER IF EXISTS sync_enrollment_grade_from_markings_trg ON marking_components;

            CREATE TRIGGER sync_enrollment_grade_from_markings_trg
            AFTER INSERT OR UPDATE OR DELETE ON marking_components
            FOR EACH ROW
            EXECUTE FUNCTION trg_sync_enrollment_grade_from_markings();

            CREATE OR REPLACE FUNCTION get_teacher_sections(p_teacher_id INT)
            RETURNS TABLE (
                section_name VARCHAR,
                department_id INT,
                department_code VARCHAR,
                department_name VARCHAR,
                course_code VARCHAR,
                course_name VARCHAR,
                term_id INT,
                term_number INT
            )
            LANGUAGE sql
            AS $$
            WITH current_term_date AS (
                SELECT newest_term_start FROM current_state LIMIT 1
            ),
            current_terms AS (
                SELECT t.id, t.term_number, t.department_id
                FROM terms t, current_term_date ctd
                WHERE t.start_date = ctd.newest_term_start
            )
            SELECT DISTINCT
                te.section_name,
                c.department_id,
                d.code AS department_code,
                d.name AS department_name,
                c.course_code,
                c.name AS course_name,
                ct.id AS term_id,
                ct.term_number
            FROM teaches te
            JOIN course_offerings co ON te.course_offering_id = co.id
            JOIN courses c ON co.course_id = c.id
            JOIN departments d ON c.department_id = d.id
            JOIN current_terms ct ON co.term_id = ct.id
            WHERE te.teacher_id = p_teacher_id
            ORDER BY d.code, c.course_code, te.section_name;
            $$;

            CREATE OR REPLACE FUNCTION get_students_in_teacher_section(
                p_teacher_id INT,
                p_section_name VARCHAR,
                p_department_id INT
            )
            RETURNS TABLE (
                user_id INT,
                name VARCHAR,
                roll_number VARCHAR,
                email VARCHAR,
                course_code VARCHAR,
                course_name VARCHAR
            )
            LANGUAGE sql
            AS $$
            WITH current_term_date AS (
                SELECT newest_term_start FROM current_state LIMIT 1
            ),
            current_terms AS (
                SELECT t.id
                FROM terms t, current_term_date ctd
                WHERE t.start_date = ctd.newest_term_start
            )
            SELECT DISTINCT
                s.user_id,
                u.name,
                s.roll_number,
                u.email,
                c.course_code,
                c.name AS course_name
            FROM teaches te
            JOIN course_offerings co ON te.course_offering_id = co.id
            JOIN courses c ON co.course_id = c.id
            JOIN student_enrollments se ON se.course_offering_id = co.id
            JOIN students s ON se.student_id = s.user_id
            JOIN users u ON s.user_id = u.id
            JOIN student_sections ss ON ss.student_id = s.user_id AND ss.section_name = te.section_name
            JOIN current_terms ct ON co.term_id = ct.id
            WHERE te.teacher_id = p_teacher_id
              AND te.section_name = p_section_name
              AND c.department_id = p_department_id
              AND se.status = 'Enrolled'
            ORDER BY s.roll_number;
            $$;

            CREATE OR REPLACE FUNCTION sync_terms_from_current_state()
            RETURNS TRIGGER
            LANGUAGE plpgsql
            AS $$
            BEGIN
                -- Keep this trigger metadata-focused.
                -- Term date synchronization is handled explicitly from application logic.
                IF TG_OP = 'INSERT' AND NEW.newest_term_start IS NULL THEN
                    NEW.newest_term_start := NEW.term_start;
                END IF;

                NEW.updated_at := CURRENT_TIMESTAMP;
                RETURN NEW;
            END;
            $$;

            CREATE OR REPLACE FUNCTION audit_current_state_changes()
            RETURNS TRIGGER
            LANGUAGE plpgsql
            AS $$
            BEGIN
                INSERT INTO current_state_history (
                    changed_by,
                    old_reg_start,
                    new_reg_start,
                    old_reg_end,
                    new_reg_end,
                    old_term_start,
                    new_term_start,
                    old_term_end,
                    new_term_end,
                    old_newest_term_start,
                    new_newest_term_start,
                    reason
                ) VALUES (
                    NEW.updated_by,
                    OLD.reg_start,
                    NEW.reg_start,
                    OLD.reg_end,
                    NEW.reg_end,
                    OLD.term_start,
                    NEW.term_start,
                    OLD.term_end,
                    NEW.term_end,
                    OLD.newest_term_start,
                    NEW.newest_term_start,
                    'Dashboard update'
                );

                RETURN NEW;
            END;
            $$;

            DROP TRIGGER IF EXISTS trg_sync_terms_from_current_state ON current_state;

            CREATE TRIGGER trg_sync_terms_from_current_state
            BEFORE INSERT OR UPDATE ON current_state
            FOR EACH ROW
            EXECUTE FUNCTION sync_terms_from_current_state();

            DROP TRIGGER IF EXISTS trg_audit_current_state_changes ON current_state;

            CREATE TRIGGER trg_audit_current_state_changes
            AFTER UPDATE ON current_state
            FOR EACH ROW
            EXECUTE FUNCTION audit_current_state_changes();

            CREATE OR REPLACE FUNCTION get_department_full_details(p_dept_identifier TEXT)
            RETURNS JSONB
            LANGUAGE plpgsql
            AS $$
            DECLARE
                v_department RECORD;
            BEGIN
                SELECT d.id, d.code, d.name
                INTO v_department
                FROM departments d
                WHERE LOWER(d.code) = LOWER(p_dept_identifier)
                   OR LOWER(d.name) = LOWER(p_dept_identifier)
                LIMIT 1;

                IF NOT FOUND THEN
                    RETURN NULL;
                END IF;

                RETURN jsonb_build_object(
                    'department', jsonb_build_object(
                        'id', v_department.id,
                        'code', v_department.code,
                        'name', v_department.name
                    ),
                    'current_department_head', (
                        SELECT jsonb_build_object(
                            'id', dh.id,
                            'department_id', dh.department_id,
                            'teacher_id', dh.teacher_id,
                            'name', u.name,
                            'email', COALESCE(t.official_mail, u.email),
                            'appointment', t.appointment,
                            'start_date', dh.start_date,
                            'end_date', dh.end_date
                        )
                        FROM department_heads dh
                        JOIN teachers t ON t.user_id = dh.teacher_id
                        JOIN users u ON u.id = t.user_id
                        WHERE dh.department_id = v_department.id
                          AND dh.end_date IS NULL
                        ORDER BY dh.start_date DESC, dh.id DESC
                        LIMIT 1
                    ),
                    'department_head_history', (
                        SELECT COALESCE(
                            jsonb_agg(
                                jsonb_build_object(
                                    'id', dh.id,
                                    'department_id', dh.department_id,
                                    'teacher_id', dh.teacher_id,
                                    'name', u.name,
                                    'email', COALESCE(t.official_mail, u.email),
                                    'appointment', t.appointment,
                                    'start_date', dh.start_date,
                                    'end_date', dh.end_date
                                )
                                ORDER BY dh.end_date DESC, dh.start_date DESC, dh.id DESC
                            ),
                            '[]'::jsonb
                        )
                        FROM department_heads dh
                        JOIN teachers t ON t.user_id = dh.teacher_id
                        JOIN users u ON u.id = t.user_id
                        WHERE dh.department_id = v_department.id
                          AND dh.end_date IS NOT NULL
                    ),
                    'courses', (
                        SELECT COALESCE(
                            jsonb_agg(
                                jsonb_build_object(
                                    'id', c.id,
                                    'code', c.course_code,
                                    'name', c.name,
                                    'type', c.type,
                                    'credit_hours', c.credit_hours
                                )
                                ORDER BY c.course_code
                            ),
                            '[]'::jsonb
                        )
                        FROM courses c
                        WHERE c.department_id = v_department.id
                    ),
                    'teachers', (
                        SELECT COALESCE(
                            jsonb_agg(
                                jsonb_build_object(
                                    'user_id', u.id,
                                    'name', u.name,
                                    'email', COALESCE(t.official_mail, u.email),
                                    'appointment', t.appointment,
                                    'designation', CASE
                                        WHEN active_head.teacher_id IS NOT NULL THEN 'Department Head'
                                        ELSE t.appointment::TEXT
                                    END
                                )
                                ORDER BY
                                    CASE
                                        WHEN active_head.teacher_id IS NOT NULL THEN 1
                                        WHEN t.appointment::TEXT = 'Professor' THEN 2
                                        WHEN t.appointment::TEXT = 'Assistant Professor' THEN 3
                                        WHEN t.appointment::TEXT = 'Lecturer' THEN 4
                                        WHEN t.appointment::TEXT = 'Associate Professor' THEN 5
                                        WHEN t.appointment::TEXT = 'Adjunct Faculty' THEN 6
                                        ELSE 7
                                    END,
                                    u.name
                            ),
                            '[]'::jsonb
                        )
                        FROM teachers t
                        JOIN users u ON u.id = t.user_id
                                    LEFT JOIN department_heads active_head
                                                 ON active_head.department_id = v_department.id
                                                AND active_head.teacher_id = t.user_id
                                                AND active_head.end_date IS NULL
                        WHERE t.department_id = v_department.id
                    ),
                    'students', (
                        SELECT COALESCE(
                            jsonb_agg(
                                jsonb_build_object(
                                    'user_id', u.id,
                                    'name', u.name,
                                    'email', COALESCE(s.official_mail, u.email),
                                    'roll_number', s.roll_number,
                                    'status', s.status,
                                    'current_term_id', tr.id,
                                    'current_term', tr.term_number
                                )
                                ORDER BY tr.term_number DESC, u.name
                            ),
                            '[]'::jsonb
                        )
                        FROM students s
                        JOIN users u ON u.id = s.user_id
                        JOIN terms tr ON tr.id = s.current_term
                        WHERE tr.department_id = v_department.id
                    ),
                    'students_by_term', (
                        SELECT COALESCE(
                            jsonb_agg(
                                jsonb_build_object(
                                    'term_id', term_data.term_id,
                                    'term_number', term_data.term_number,
                                    'students', term_data.students
                                )
                                ORDER BY term_data.term_number DESC
                            ),
                            '[]'::jsonb
                        )
                        FROM (
                            SELECT
                                tr.id AS term_id,
                                tr.term_number,
                                COALESCE(
                                    jsonb_agg(
                                        jsonb_build_object(
                                            'user_id', u.id,
                                            'name', u.name,
                                            'email', COALESCE(s.official_mail, u.email),
                                            'roll_number', s.roll_number,
                                            'status', s.status,
                                            'current_term', tr.term_number
                                        )
                                        ORDER BY u.name
                                    ) FILTER (WHERE s.user_id IS NOT NULL),
                                    '[]'::jsonb
                                ) AS students
                            FROM terms tr
                            LEFT JOIN students s ON s.current_term = tr.id
                            LEFT JOIN users u ON u.id = s.user_id
                            WHERE tr.department_id = v_department.id
                            GROUP BY tr.id, tr.term_number
                        ) AS term_data
                    )
                );
            END;
            $$;
        `;
        return await runWithLogging(() => this.db.query(sql), "Creating Functions and Triggers");
    }

    async initalizeTables() {
        return await runWithLogging(() => this.db.query("SELECT 1"), "Starting Table Initialization", async () => {
            await this.createEnums();
            await this.createCoreUsers();
            await this.createOrganizations();
            await this.createAcademics();
            await this.createGrading();
            await this.createDues();
            await this.createFeedback();
            await this.createSystemState();
            await this.createFunctions();
            return { status: "All tables initialized successfully" };
        });
    }

    testConnection = async (req, res) => {
        const query = ' SELECT 1 ;'
        return await runWithLogging(
            () => this.db.query(query),
            'DB connection test'
        )
    }
}

module.exports = TableModel;